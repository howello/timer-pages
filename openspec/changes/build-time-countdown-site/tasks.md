## 1. 项目骨架与基础设施

- [x] 1.1 创建目录结构：根目录 HTML 页面、`css/`、`js/`、`js/lib/`（本地兜底可选）
- [ ] 1.2 建立 CDN 引入清单（`lunar-javascript`、`aliyun-oss-sdk-6.18.0`），在页面中引入
- [ ] 1.3 从 `docs/fluffy-time-design/css/fluffy.css` 抽取并落地基础设计系统样式到 `css/`（毛玻璃/奶油/新拟态变量与组件）
- [ ] 1.4 建立运行期配置读取方案（Cloudflare Pages 环境变量注入：密码、OSS 参数）与 `config.js` 占位

## 2. 密码访问控制（access-gate）

- [x] 2.1 创建 `password.html`（根目录），落地毛玻璃密码卡片与显示/隐藏切换
- [x] 2.2 实现 `js/access-gate.js`：校验输入密码是否等于环境变量注入的密码
- [x] 2.3 校验通过后写入会话标记（sessionStorage）并跳转主页；主页在无标记时重定向回密码页
- [x] 2.4 密码错误提示与边界处理（空输入、连续错误）

## 3. 事件数据模型与时间计算（event-cards）

- [x] 3.1 定义 4 类事件的 JSON 数据模型（节日/倒计时/周期性节日/已过天数 + 公历/农历字段）
- [x] 3.2 实现 `js/time-calc.js`：倒计时（目标未来）、正计时（已过天数）、周期循环（取下一次发生日）计算
- [x] 3.3 集成 `lunar-javascript`：农历 → 公历换算，周期性农历事件求下一次公历日期
- [x] 3.4 走动时间渲染（天/时/分/秒），每秒刷新
- [x] 3.5 卡片渲染函数：由配置数据生成 DOM，不写死

## 4. 节假日数据接入（holiday-data）

- [x] 4.1 实现 `js/holiday.js`：请求 `api.jiejiariapi.com/v1/holidays/{year}`
- [x] 4.2 按 name 分组，取每组最早日期作为节日代表日
- [x] 4.3 法定节假日判定（isOffDay）与高速免费标注（仅春节、清明、劳动节、国庆节）
- [x] 4.4 API 失败降级处理（提示 + 空数据兜底）

## 5. OSS 存储读写（oss-storage）

- [x] 5.1 实现 `js/oss-storage.js`：用 aliyun-oss-sdk 初始化 client（参数来自注入配置）
- [x] 5.2 读取 OSS 上的事件配置 JSON（参考 download 示例）
- [x] 5.3 覆盖写回事件配置 JSON（参考 upload 示例）
- [x] 5.4 读取失败/写入失败的降级与错误提示

## 6. 主页交互体验（home-experience）

- [x] 6.1 创建 `index.html`（根目录）主页结构：固定卡片区 + 顶部标题栏 + 下方列表
- [x] 6.2 初始只展示固定卡片；实现滚动触发标题栏按钮与列表的动画过渡浮现
- [x] 6.3 列表卡片置顶功能（置顶项排前 + 状态持久化到配置）
- [x] 6.4 列表卡片拖拽排序（排序结果持久化到配置）
- [x] 6.5 新增事件弹窗（在原型基础上优化外观），表单支持 4 类事件 + 公历/农历切换
- [x] 6.6 新增事件提交 → 更新配置 → 回写 OSS → 刷新卡片渲染
- [x] 6.7 PC/手机端响应式适配验证

## 7. 部署手册（deployment-guide）

- [x] 7.1 编写 Cloudflare Pages 部署步骤（连接仓库、构建设置、环境变量配置）
- [x] 7.2 编写阿里云 OSS 配置步骤（Bucket 创建、CORS、RAM 子账号最小权限策略、仅限指定 JSON 读写）
- [x] 7.3 编写环境变量清单（密码、OSS region/bucket/AK/SK 等）与注入说明
- [x] 7.4 编写初始 OSS JSON 文件示例与上线验证清单

## 8. 集成验证

- [x] 8.1 端到端流程验证：密码 → 主页 → 节日/自定义卡片渲染 → 新增回写 → 刷新保留
- [x] 8.2 农历事件计算正确性验证
- [x] 8.3 响应式与降级场景验证（API 失败、OSS 失败、密码错误）
